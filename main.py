#State class, provides governor, etc.

def main():
    """ On a a large scale, this is the State.
        It spawns cities and does as it pleases with them. """
    # First off, we want to create a city with some number of agents with a certain spread of
    # characteristics. Then we want to let it run until we can tell that it has stagnated
    # On second thought, we'll just cut it off after a set number of iterations
    # Actually, we should have used a multi-line comment for this.
    """ ah, well. """
    
    c = City()
    
    
if __name__ == '__main__':
    main()
